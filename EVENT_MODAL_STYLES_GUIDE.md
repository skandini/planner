# 📋 Инструкция по стилям модального окна создания события (ветка testmain-copy)

Данная инструкция описывает все стили модального окна создания события и раздела участников из ветки `testmain-copy` для воспроизведения в будущем.

---

## 🎨 Общая структура модального окна

### 1. Оверлей (фон модального окна)

```tsx
<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4">
  {/* Контент модального окна */}
</div>
```

**Стили:**
- `fixed inset-0` - фиксированное позиционирование на весь экран
- `z-50` - высокий z-index для отображения поверх других элементов
- `flex items-center justify-center` - центрирование содержимого
- `bg-black/40` - черный фон с прозрачностью 40%
- `p-4` - внутренние отступы 16px со всех сторон

---

### 2. Контейнер модального окна

```tsx
<div className="w-full max-w-5xl max-h-[95vh] overflow-hidden rounded-xl border border-slate-200 bg-white shadow-2xl flex flex-col">
  {/* Содержимое */}
</div>
```

**Стили:**
- `w-full` - ширина 100%
- `max-w-5xl` - максимальная ширина 64rem (1024px)
- `max-h-[95vh]` - максимальная высота 95% высоты экрана
- `overflow-hidden` - скрытие переполнения
- `rounded-xl` - скругление углов 12px
- `border border-slate-200` - граница цвета slate-200
- `bg-white` - белый фон
- `shadow-2xl` - большая тень
- `flex flex-col` - вертикальное расположение flex-элементов

---

## 📌 Заголовок модального окна

```tsx
<div className="border-b border-slate-200 bg-white px-5 py-3 flex items-center justify-between flex-shrink-0">
  {/* Заголовок и кнопки */}
</div>
```

**Стили:**
- `border-b border-slate-200` - нижняя граница
- `bg-white` - белый фон
- `px-5 py-3` - внутренние отступы (px: 20px, py: 12px)
- `flex items-center justify-between` - горизонтальное расположение с пространством между
- `flex-shrink-0` - запрет сжатия

### Заголовок текста

```tsx
<h2 className="text-lg font-semibold text-slate-900 truncate">
  {isEditing ? "Редактировать событие" : "Новое событие"}
</h2>
```

**Стили:**
- `text-lg` - размер текста 18px
- `font-semibold` - жирность 600
- `text-slate-900` - цвет текста
- `truncate` - обрезка длинного текста

### Название календаря

```tsx
<span className="text-xs text-slate-500 truncate">• {calendarName}</span>
```

**Стили:**
- `text-xs` - размер текста 12px
- `text-slate-500` - серый цвет текста
- `truncate` - обрезка текста

### Кнопка сохранения

```tsx
<button className="rounded-lg bg-lime-500 px-4 py-1.5 text-sm font-medium text-white transition hover:bg-lime-600 disabled:opacity-50 disabled:cursor-not-allowed">
  {isEditing ? "Сохранить" : "Создать"}
</button>
```

**Стили:**
- `rounded-lg` - скругление углов 8px
- `bg-lime-500` - фон lime-500 (#84cc16)
- `px-4 py-1.5` - внутренние отступы (px: 16px, py: 6px)
- `text-sm` - размер текста 14px
- `font-medium` - жирность 500
- `text-white` - белый текст
- `transition` - плавная анимация
- `hover:bg-lime-600` - фон при наведении lime-600
- `disabled:opacity-50` - прозрачность 50% в disabled
- `disabled:cursor-not-allowed` - курсор "не разрешено" в disabled

### Кнопка закрытия

```tsx
<button className="rounded-lg p-1.5 text-slate-400 transition hover:bg-slate-100 hover:text-slate-600">
  {/* Иконка X */}
</button>
```

**Стили:**
- `rounded-lg` - скругление углов 8px
- `p-1.5` - внутренние отступы 6px
- `text-slate-400` - серый цвет текста
- `transition` - плавная анимация
- `hover:bg-slate-100` - фон при наведении
- `hover:text-slate-600` - цвет текста при наведении

---

## 📝 Контент формы

### Контейнер формы

```tsx
<div className="flex-1 overflow-y-auto">
  <form className="p-5 space-y-4">
    {/* Поля формы */}
  </form>
</div>
```

**Стили:**
- `flex-1` - занимает всё доступное пространство
- `overflow-y-auto` - вертикальная прокрутка при необходимости
- `p-5` - внутренние отступы 20px
- `space-y-4` - вертикальные отступы между детьми 16px

---

## 🏷️ Поля формы

### Название события

```tsx
<label className="block mb-1">
  <span className="text-xs font-medium text-slate-700">
    Название <span className="text-red-500">*</span>
  </span>
</label>
<input className="w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm text-slate-900 placeholder-slate-400 transition focus:border-lime-500 focus:ring-1 focus:ring-lime-500/20" />
```

**Стили label:**
- `block` - блочный элемент
- `mb-1` - нижний отступ 4px

**Стили input:**
- `w-full` - ширина 100%
- `rounded-lg` - скругление углов 8px
- `border border-slate-200` - граница
- `bg-white` - белый фон
- `px-3 py-2` - внутренние отступы (px: 12px, py: 8px)
- `text-sm` - размер текста 14px
- `text-slate-900` - цвет текста
- `placeholder-slate-400` - цвет placeholder
- `transition` - плавная анимация
- `focus:border-lime-500` - граница при фокусе lime-500
- `focus:ring-1` - кольцо при фокусе толщиной 1px
- `focus:ring-lime-500/20` - цвет кольца при фокусе lime-500 с прозрачностью 20%

### Дата и время (Начало/Конец)

```tsx
<input type="datetime-local" className="w-full rounded-lg border border-slate-200 bg-white px-2.5 py-2 text-xs text-slate-900 transition focus:border-lime-500 focus:ring-1 focus:ring-lime-500/20" />
```

**Стили:**
- `text-xs` - размер текста 12px (меньше чем у названия)
- Остальные стили аналогичны полю "Название"

### Описание

```tsx
<textarea className="w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm text-slate-900 placeholder-slate-400 transition focus:border-lime-500 focus:ring-1 focus:ring-lime-500/20 resize-none" rows={2} />
```

**Стили:**
- `resize-none` - запрет изменения размера
- `rows={2}` - 2 строки по умолчанию
- Остальные стили аналогичны полю "Название"

---

## 👥 Раздел участников (ParticipantSearch - компактный режим)

### Контейнер участников

```tsx
<div className="flex items-center gap-2 flex-wrap">
  <span className="text-sm font-semibold text-slate-700">Участники:</span>
  {/* Чипсы участников */}
</div>
```

**Стили контейнера:**
- `flex items-center` - горизонтальное расположение с выравниванием по центру
- `gap-2` - отступ между элементами 8px
- `flex-wrap` - перенос на новую строку при необходимости

**Стили метки:**
- `text-sm` - размер текста 14px
- `font-semibold` - жирность 600
- `text-slate-700` - цвет текста

### Чип участника (выбранный)

```tsx
<div className="group flex items-center gap-1.5 rounded-full border border-lime-200 bg-lime-50 px-2 py-1 pr-1.5 transition hover:border-lime-300 hover:bg-lime-100">
  {/* Аватар */}
  {/* Имя */}
  {/* Организация */}
  {/* Кнопка удаления */}
</div>
```

**Стили:**
- `group` - группа для hover-эффектов
- `flex items-center` - горизонтальное расположение
- `gap-1.5` - отступ между элементами 6px
- `rounded-full` - полностью скругленные углы (круг)
- `border border-lime-200` - граница lime-200
- `bg-lime-50` - фон lime-50
- `px-2 py-1` - внутренние отступы (px: 8px, py: 4px)
- `pr-1.5` - правый отступ 6px (для кнопки удаления)
- `transition` - плавная анимация
- `hover:border-lime-300` - граница при наведении
- `hover:bg-lime-100` - фон при наведении

### Аватар участника

```tsx
{/* Если есть фото */}
<img className="h-5 w-5 rounded-full object-cover border border-white shadow-sm" />

{/* Если нет фото - инициалы */}
<div className="flex h-5 w-5 items-center justify-center rounded-full bg-gradient-to-br from-lime-400 to-emerald-500 text-[0.6rem] font-semibold text-white">
  {initial}
</div>
```

**Стили изображения:**
- `h-5 w-5` - размер 20x20px
- `rounded-full` - круглый
- `object-cover` - покрытие области
- `border border-white` - белая граница
- `shadow-sm` - маленькая тень

**Стили инициалов:**
- `flex items-center justify-center` - центрирование
- `h-5 w-5` - размер 20x20px
- `rounded-full` - круглый
- `bg-gradient-to-br from-lime-400 to-emerald-500` - градиент от lime-400 к emerald-500
- `text-[0.6rem]` - размер текста 9.6px
- `font-semibold` - жирность 600
- `text-white` - белый текст

### Имя участника

```tsx
<span className="text-xs font-medium text-slate-900 max-w-[120px] truncate">
  {name}
</span>
```

**Стили:**
- `text-xs` - размер текста 12px
- `font-medium` - жирность 500
- `text-slate-900` - цвет текста
- `max-w-[120px]` - максимальная ширина 120px
- `truncate` - обрезка длинного текста

### Бейдж организации

```tsx
<span className="rounded-full bg-slate-200 px-1 py-0.5 text-[0.55rem] font-semibold text-slate-700">
  {orgAbbr}
</span>
```

**Стили:**
- `rounded-full` - круглый
- `bg-slate-200` - фон slate-200
- `px-1 py-0.5` - внутренние отступы (px: 4px, py: 2px)
- `text-[0.55rem]` - размер текста 8.8px
- `font-semibold` - жирность 600
- `text-slate-700` - цвет текста

### Кнопка удаления участника

```tsx
<button className="ml-0.5 flex h-4 w-4 items-center justify-center rounded-full text-slate-400 transition hover:bg-lime-200 hover:text-slate-600">
  {/* Иконка X */}
</button>
```

**Стили:**
- `ml-0.5` - левый отступ 2px
- `flex items-center justify-center` - центрирование
- `h-4 w-4` - размер 16x16px
- `rounded-full` - круглый
- `text-slate-400` - цвет иконки
- `transition` - плавная анимация
- `hover:bg-lime-200` - фон при наведении
- `hover:text-slate-600` - цвет при наведении

### Кнопка добавления участника

```tsx
<button className="flex items-center gap-1.5 rounded-full border border-dashed border-slate-300 bg-white px-2.5 py-1 text-xs font-medium text-slate-600 transition hover:border-lime-400 hover:bg-lime-50 hover:text-lime-700">
  {/* Иконка + */}
  <span>Добавить</span>
</button>
```

**Стили:**
- `flex items-center` - горизонтальное расположение
- `gap-1.5` - отступ между иконкой и текстом 6px
- `rounded-full` - круглый
- `border border-dashed border-slate-300` - пунктирная граница
- `bg-white` - белый фон
- `px-2.5 py-1` - внутренние отступы (px: 10px, py: 4px)
- `text-xs` - размер текста 12px
- `font-medium` - жирность 500
- `text-slate-600` - цвет текста
- `transition` - плавная анимация
- `hover:border-lime-400` - граница при наведении
- `hover:bg-lime-50` - фон при наведении
- `hover:text-lime-700` - цвет текста при наведении

### Выпадающий список поиска участников

```tsx
<div className="absolute z-20 mt-2 left-0 max-h-64 w-80 overflow-y-auto rounded-lg border border-slate-200 bg-white shadow-lg">
  {/* Поле поиска */}
  {/* Список результатов */}
</div>
```

**Стили:**
- `absolute` - абсолютное позиционирование
- `z-20` - z-index 20
- `mt-2` - верхний отступ 8px
- `left-0` - прижато к левому краю
- `max-h-64` - максимальная высота 256px
- `w-80` - ширина 320px
- `overflow-y-auto` - вертикальная прокрутка
- `rounded-lg` - скругление углов 8px
- `border border-slate-200` - граница
- `bg-white` - белый фон
- `shadow-lg` - большая тень

### Поле поиска в выпадающем списке

```tsx
<input className="w-full rounded-lg border border-slate-200 bg-white py-2 px-3 text-sm text-slate-900 placeholder-slate-400 transition-all focus:border-lime-500 focus:ring-2 focus:ring-lime-500/20 mb-2" />
```

**Стили:**
- `w-full` - ширина 100%
- `rounded-lg` - скругление углов 8px
- `border border-slate-200` - граница
- `bg-white` - белый фон
- `py-2 px-3` - внутренние отступы (py: 8px, px: 12px)
- `text-sm` - размер текста 14px
- `text-slate-900` - цвет текста
- `placeholder-slate-400` - цвет placeholder
- `transition-all` - плавная анимация всех свойств
- `focus:border-lime-500` - граница при фокусе
- `focus:ring-2` - кольцо при фокусе толщиной 2px
- `focus:ring-lime-500/20` - цвет кольца
- `mb-2` - нижний отступ 8px

### Элемент в списке результатов

```tsx
<button className="w-full flex items-center gap-2 rounded-lg px-2 py-2 text-left transition hover:bg-slate-50">
  {/* Аватар */}
  {/* Имя и email */}
</button>
```

**Стили:**
- `w-full` - ширина 100%
- `flex items-center` - горизонтальное расположение
- `gap-2` - отступ между элементами 8px
- `rounded-lg` - скругление углов 8px
- `px-2 py-2` - внутренние отступы 8px
- `text-left` - выравнивание текста влево
- `transition` - плавная анимация
- `hover:bg-slate-50` - фон при наведении

---

## 🎯 Разделители секций

### Горизонтальный разделитель

```tsx
<div className="border-t border-slate-200 pt-4">
  {/* Контент секции */}
</div>
```

**Стили:**
- `border-t border-slate-200` - верхняя граница
- `pt-4` - верхний отступ 16px

---

## 🎨 Цветовая палитра

### Основные цвета (lime - зеленый)
- `lime-50` - `#f7fee7` - очень светлый фон
- `lime-100` - `#ecfccb` - светлый фон при hover
- `lime-200` - `#d9f99d` - границы
- `lime-300` - `#bef264` - границы при hover
- `lime-400` - `#a3e635` - градиенты
- `lime-500` - `#84cc16` - основной цвет кнопок
- `lime-600` - `#65a30d` - hover состояния
- `lime-700` - `#4a7c0f` - hover текста

### Цвета slate (серый)
- `slate-50` - `#f8fafc` - фон
- `slate-100` - `#f1f5f9` - фон при hover
- `slate-200` - `#e2e8f0` - границы
- `slate-300` - `#cbd5e1` - границы пунктирные
- `slate-400` - `#94a3b8` - иконки, placeholder
- `slate-500` - `#64748b` - вторичный текст
- `slate-600` - `#475569` - hover текста
- `slate-700` - `#334155` - метки
- `slate-900` - `#0f172a` - основной текст

### Дополнительные цвета
- `emerald-500` - `#10b981` - градиенты
- `red-500` - `#ef4444` - обязательные поля (*)
- `amber-200` - `#fde68a` - предупреждения
- `amber-50` - `#fffbeb` - фон предупреждений
- `amber-800` - `#92400e` - текст предупреждений

---

## 📏 Отступы и размеры

### Стандартные отступы
- `p-1` - 4px
- `p-1.5` - 6px
- `p-2` - 8px
- `p-3` - 12px
- `p-4` - 16px
- `p-5` - 20px

### Размеры текста
- `text-[0.55rem]` - 8.8px (бейджи)
- `text-[0.6rem]` - 9.6px (инициалы в аватаре)
- `text-xs` - 12px (вспомогательный текст)
- `text-sm` - 14px (обычный текст)
- `text-lg` - 18px (заголовки)

### Размеры элементов
- Аватар компактный: `h-5 w-5` (20x20px)
- Аватар в списке: `h-6 w-6` (24x24px)
- Аватар полный режим: `h-8 w-8` (32x32px)
- Кнопка удаления: `h-4 w-4` (16x16px)

---

## ✅ Чеклист для воспроизведения

При создании нового модального окна или обновлении существующего, убедитесь что:

- [ ] Оверлей использует `bg-black/40`
- [ ] Контейнер модального окна имеет `max-w-5xl`, `rounded-xl`, `shadow-2xl`
- [ ] Заголовок имеет `border-b border-slate-200`, `px-5 py-3`
- [ ] Кнопка сохранения использует `bg-lime-500`, `hover:bg-lime-600`
- [ ] Все поля формы имеют `focus:border-lime-500 focus:ring-1 focus:ring-lime-500/20`
- [ ] Чипсы участников используют `border-lime-200 bg-lime-50`, `rounded-full`
- [ ] Аватары используют градиент `from-lime-400 to-emerald-500`
- [ ] Кнопка добавления имеет `border-dashed border-slate-300`
- [ ] Разделители используют `border-t border-slate-200 pt-4`
- [ ] Все hover эффекты используют transition
- [ ] Все размеры текста соответствуют спецификации
- [ ] Все отступы соответствуют спецификации

---

## 📝 Примечания

1. **Компактный режим участников**: В модальном окне используется компактный режим ParticipantSearch (`compact={true}`)

2. **Цветовая схема**: Основной акцентный цвет - `lime-500` (#84cc16), используется для всех интерактивных элементов

3. **Тени**: Модальное окно использует `shadow-2xl`, выпадающие списки - `shadow-lg`

4. **Скругления**: Основные элементы используют `rounded-lg` (8px), чипсы - `rounded-full` (полностью круглые)

5. **Переходы**: Все интерактивные элементы должны иметь `transition` для плавной анимации

---

**Ветка:** `testmain-copy`  
**Файлы:** 
- `frontend/src/components/events/EventModalEnhanced.tsx`
- `frontend/src/components/participants/ParticipantSearch.tsx`
- `frontend/src/components/rooms/ResourcePanel.tsx`

