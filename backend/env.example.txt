# Скопируйте этот файл в .env и настройте значения
# cp env.example.txt .env

# Database
# Для SQLite (разработка):
DATABASE_URL=sqlite:///./calendar.db

# Для PostgreSQL (продакшен):
# DATABASE_URL=postgresql://username:password@localhost/planner_db

# Security
# Сгенерируйте секретный ключ: openssl rand -hex 32
SECRET_KEY=changeme-change-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=30

# CORS
# Разрешенные домены для CORS (JSON массив или через запятую)
# Вариант 1: JSON массив (рекомендуется)
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:3001","http://127.0.0.1:3000","http://127.0.0.1:3001"]
# Вариант 2: Через запятую (также поддерживается)
# BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:3001,https://yourdomain.com

# Redis (для локальной разработки, по умолчанию уже настроено в config.py)
# Примечание: Redis и Celery НЕ обязательны для работы backend!
# Backend будет работать без Redis/Celery, но уведомления не будут отправляться.
# REDIS_URL=redis://localhost:6379/0
# REDIS_CACHE_URL=redis://localhost:6379/1
# CELERY_BROKER_URL=redis://localhost:6379/0
# CELERY_RESULT_BACKEND=redis://localhost:6379/0

