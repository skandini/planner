# 🔄 Обновление продакшена после конфликта package-lock.json

## Проблема
При выполнении `git pull origin testmain` появляется ошибка:
```
error: Your local changes to the following files would be overwritten by merge:
        frontend/package-lock.json
Please commit your changes or stash them before you merge.
```

## Причина
На продакшене есть локальные изменения в `package-lock.json`, которые были созданы при локальной установке зависимостей (`npm install`). Это нормально - `package-lock.json` может отличаться на разных системах.

## Решение

### Вариант 1 (рекомендуется): Сохранить изменения в stash

```bash
cd /opt/planner

# Сохранить локальные изменения во временное хранилище
git stash

# Обновить код из репозитория
git pull origin testmain

# Перейти в frontend и переустановить зависимости
cd frontend
rm -rf node_modules package-lock.json
npm install

# Пересобрать проект
npm run build

# Вернуться в корень и перезапустить сервисы
cd ..
sudo systemctl restart planner-frontend
sudo systemctl status planner-frontend
```

### Вариант 2: Отменить локальные изменения

```bash
cd /opt/planner

# Отменить локальные изменения в package-lock.json
cd frontend
git checkout -- package-lock.json

# Вернуться в корень и обновить код
cd ..
git pull origin testmain

# Переустановить зависимости
cd frontend
rm -rf node_modules
npm install

# Пересобрать проект
npm run build

# Вернуться в корень и перезапустить сервисы
cd ..
sudo systemctl restart planner-frontend
sudo systemctl status planner-frontend
```

### Вариант 3: Использовать скрипт деплоя

```bash
cd /opt/planner

# Использовать скрипт деплоя, который автоматически обработает конфликты
./scripts/deploy.sh update testmain
```

## Полная последовательность команд (Вариант 1)

```bash
# 1. Сохранить локальные изменения
cd /opt/planner
git stash

# 2. Обновить код
git pull origin testmain

# 3. Пересобрать frontend
cd frontend
rm -rf .next node_modules package-lock.json
npm install
npm run build

# 4. Перезапустить сервисы
cd ..
sudo systemctl restart planner-frontend
sudo systemctl restart planner-backend

# 5. Проверить статус
sudo systemctl status planner-frontend
sudo systemctl status planner-backend

# 6. Проверить логи (если нужно)
sudo journalctl -u planner-frontend -n 50 --no-pager
sudo journalctl -u planner-backend -n 50 --no-pager
```

## Проверка после обновления

```bash
# Проверить что сервисы работают
sudo systemctl is-active planner-frontend && echo "✓ Frontend активен" || echo "✗ Frontend не работает"
sudo systemctl is-active planner-backend && echo "✓ Backend активен" || echo "✗ Backend не работает"

# Проверить версии
cd /opt/planner
git log -1 --oneline

# Проверить что сборка существует
[ -d "/opt/planner/frontend/.next" ] && echo "✓ Сборка существует" || echo "✗ Сборка отсутствует"
```

## Если возникли ошибки

### Ошибка при npm install
```bash
cd /opt/planner/frontend
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### Ошибка при сборке
```bash
cd /opt/planner/frontend
npm run build 2>&1 | tee build.log
tail -50 build.log
```

### Сервис не запускается
```bash
# Проверить логи
sudo journalctl -u planner-frontend -n 100 --no-pager

# Проверить права доступа
ls -la /opt/planner/frontend/.next

# Проверить конфигурацию сервиса
sudo systemctl cat planner-frontend
```

## Примечания

- `package-lock.json` должен быть одинаковым в репозитории и на продакшене
- После `npm install` может появиться конфликт, но это нормально - просто используйте версию из репозитория
- Всегда проверяйте статус сервисов после обновления
- Проверяйте логи при возникновении проблем

