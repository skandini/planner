# 🔍 Пошаговая проверка различий между локальной и продакшен средой

## Шаг 1: Проверка версий зависимостей

### Локально:
```powershell
cd frontend
npm list next react react-dom tailwindcss --depth=0
```

**Ожидаемый результат:**
- next@16.0.3
- react@19.2.0
- react-dom@19.2.0
- tailwindcss@4.1.17

### На проде (выполните на сервере):
```bash
cd /path/to/frontend
npm list next react react-dom tailwindcss --depth=0
```

**Сравните версии!** Если версии отличаются - это причина проблемы.

---

## Шаг 2: Проверка package.json и package-lock.json

### Локально:
```powershell
# Проверьте содержимое package.json
Get-Content frontend\package.json

# Проверьте хеш package-lock.json
Get-FileHash frontend\package-lock.json
```

### На проде:
```bash
# Проверьте содержимое package.json
cat frontend/package.json

# Проверьте хеш package-lock.json
md5sum frontend/package-lock.json
```

**Если package-lock.json отличается** - выполните на проде:
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
```

---

## Шаг 3: Проверка исходного кода

### Сравните ключевые файлы:

1. **globals.css** - стили должны быть одинаковые
2. **layout.tsx** - структура должна совпадать
3. **page.tsx** - основной компонент
4. **next.config.ts** - конфигурация Next.js
5. **tailwind.config.ts** - конфигурация Tailwind (если есть)

### На проде проверьте:
```bash
# Проверьте последний коммит
cd /path/to/project
git log -1 --oneline

# Проверьте, нет ли незакоммиченных изменений
git status

# Проверьте, какая ветка активна
git branch
```

**Убедитесь, что на проде используется та же ветка и последние изменения!**

---

## Шаг 4: Проверка сборки

### Локально создайте production build:
```powershell
cd frontend
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue
npm run build
```

### Проверьте размеры файлов:
```powershell
# Размер .next папки
(Get-ChildItem .next -Recurse | Measure-Object -Property Length -Sum).Sum / 1MB

# Размер основных CSS файлов
Get-ChildItem .next\static\css\*.css | Select-Object Name, @{Name="Size(KB)";Expression={[math]::Round($_.Length/1KB,2)}}
```

### На проде проверьте:
```bash
cd frontend
# Удалите старую сборку
rm -rf .next

# Пересоберите
npm run build

# Проверьте размеры
du -sh .next
du -sh .next/static/css
```

**Сравните размеры!** Если сильно отличаются - возможно, не все файлы собрались.

---

## Шаг 5: Проверка CSS файлов

### Локально:
```powershell
# Найдите все CSS файлы в сборке
Get-ChildItem frontend\.next\static\css\*.css | Select-Object Name, Length

# Проверьте содержимое основного CSS файла
Get-Content frontend\.next\static\css\*.css | Select-String "tailwind" | Select-Object -First 5
```

### На проде:
```bash
# Найдите все CSS файлы
find frontend/.next/static/css -name "*.css" -exec ls -lh {} \;

# Проверьте содержимое
grep -i "tailwind" frontend/.next/static/css/*.css | head -5
```

**Если CSS файлы пустые или маленькие** - Tailwind не генерирует стили!

---

## Шаг 6: Проверка в браузере

### Откройте DevTools (F12) и проверьте:

1. **Network tab:**
   - Загружаются ли CSS файлы?
   - Какой размер у CSS файлов?
   - Есть ли ошибки 404?

2. **Console tab:**
   - Есть ли ошибки JavaScript?
   - Есть ли предупреждения о стилях?

3. **Elements tab:**
   - Найдите элемент с Tailwind классами (например, `bg-slate-50`)
   - Проверьте Computed styles - применяются ли стили?
   - Проверьте, какие CSS правила применяются

4. **Sources tab:**
   - Найдите загруженные CSS файлы
   - Проверьте их содержимое - есть ли там Tailwind классы?

---

## Шаг 7: Проверка переменных окружения

### Локально:
```powershell
# Проверьте .env.local
Get-Content frontend\.env.local -ErrorAction SilentlyContinue
```

### На проде:
```bash
# Проверьте переменные окружения
cat frontend/.env.local
# или
env | grep NEXT_PUBLIC
```

**Убедитесь, что переменные одинаковые!**

---

## Шаг 8: Проверка Node.js версии

### Локально:
```powershell
node --version
npm --version
```

### На проде:
```bash
node --version
npm --version
```

**Версии должны совпадать!** Next.js 16 требует Node.js 18.17+

---

## 🚨 Частые проблемы и решения:

### Проблема 1: Разные версии зависимостей
**Решение:**
```bash
# На проде
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run build
```

### Проблема 2: Старая версия кода на проде
**Решение:**
```bash
# На проде
cd /path/to/project
git pull origin main  # или ваша ветка
cd frontend
npm install
npm run build
```

### Проблема 3: Tailwind не генерирует стили
**Решение:**
```bash
# На проде
cd frontend
rm -rf .next node_modules
npm install
npm run build
# Проверьте, что tailwind.config.ts существует
```

### Проблема 4: Кэш браузера
**Решение:**
- Очистите кэш браузера (Ctrl+Shift+Delete)
- Или откройте в режиме инкогнито
- Или добавьте `?v=2` к URL для принудительной перезагрузки

### Проблема 5: Разные версии Node.js
**Решение:**
- Используйте nvm для управления версиями Node.js
- Убедитесь, что на проде та же версия, что и локально

---

## 📋 Чеклист для быстрой проверки:

- [ ] Версии зависимостей одинаковые (npm list)
- [ ] package-lock.json одинаковый (сравнить хеши)
- [ ] Исходный код одинаковый (git status, git log)
- [ ] Сборка выполнена заново (rm -rf .next && npm run build)
- [ ] CSS файлы не пустые (проверить размеры)
- [ ] Tailwind классы генерируются (grep в CSS файлах)
- [ ] Node.js версия одинаковая
- [ ] Переменные окружения одинаковые
- [ ] Кэш браузера очищен

---

## 🔧 Скрипт для автоматической проверки (на проде):

Создайте файл `check-frontend.sh`:
```bash
#!/bin/bash
echo "=== Проверка версий ==="
npm list next react react-dom tailwindcss --depth=0

echo -e "\n=== Проверка Node.js ==="
node --version
npm --version

echo -e "\n=== Проверка Git ==="
git log -1 --oneline
git status

echo -e "\n=== Проверка сборки ==="
if [ -d ".next" ]; then
    echo ".next существует"
    du -sh .next
else
    echo ".next НЕ существует - нужно собрать!"
fi

echo -e "\n=== Проверка CSS ==="
if [ -d ".next/static/css" ]; then
    find .next/static/css -name "*.css" -exec ls -lh {} \;
else
    echo "CSS файлы не найдены!"
fi
```

Запустите:
```bash
chmod +x check-frontend.sh
./check-frontend.sh
```

