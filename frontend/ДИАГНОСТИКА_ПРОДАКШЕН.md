# 🔍 Диагностика различий дизайна между локальной и продакшен средой

## ⚠️ Проблема
Весь дизайн разный на проде и локально - похоже, что загружены разные версии.

## 📋 Пошаговая проверка

### Шаг 1: Проверка версий зависимостей

**На проде выполните:**
```bash
cd /path/to/frontend
npm list next react react-dom tailwindcss --depth=0
```

**Сравните с локальной версией:**
```powershell
cd frontend
npm list next react react-dom tailwindcss --depth=0
```

**Должны совпадать:**
- next@16.0.3
- react@19.2.0
- react-dom@19.2.0
- tailwindcss@4.1.17

**Если версии отличаются:**
```bash
# На проде
cd frontend
rm -rf node_modules package-lock.json
npm install
```

---

### Шаг 2: Проверка исходного кода

**На проде проверьте:**
```bash
cd /path/to/project
git log -1 --oneline
git status
git branch
```

**Убедитесь, что:**
- Используется правильная ветка (например, `testmain`)
- Нет незакоммиченных изменений
- Последний коммит совпадает с локальной версией

**Если код отличается:**
```bash
git pull origin testmain  # или ваша ветка
cd frontend
npm install
```

---

### Шаг 3: Проверка конфигурационных файлов

**На проде проверьте наличие файлов:**
```bash
cd frontend
ls -la next.config.ts tailwind.config.ts postcss.config.mjs tsconfig.json
```

**Все файлы должны существовать!**

**Если `tailwind.config.ts` отсутствует:**
```bash
# Скопируйте с локальной машины или создайте заново
cat > tailwind.config.ts << 'EOF'
import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
};

export default config;
EOF
```

---

### Шаг 4: Пересборка проекта

**На проде выполните полную пересборку:**
```bash
cd frontend

# Удалите старую сборку
rm -rf .next node_modules/.cache

# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install

# Пересоберите
npm run build

# Проверьте, что сборка прошла успешно
if [ -d ".next" ]; then
    echo "✓ Сборка успешна"
    du -sh .next
else
    echo "✗ Ошибка сборки!"
    exit 1
fi
```

---

### Шаг 5: Проверка CSS файлов

**В Next.js 16 с Tailwind CSS v4 стили могут быть:**
1. Встроены в JavaScript файлы
2. В `.next/app` папке
3. В `.next/static/chunks`

**Проверьте на проде:**
```bash
cd frontend

# Найдите все CSS файлы
find .next -name "*.css" -type f

# Проверьте размеры
find .next -name "*.css" -type f -exec ls -lh {} \;

# Проверьте содержимое (должны быть Tailwind классы)
find .next -name "*.css" -type f -exec head -20 {} \;
```

**Если CSS файлы пустые или отсутствуют:**
- Tailwind не генерирует стили
- Проверьте `postcss.config.mjs`
- Проверьте `globals.css`

---

### Шаг 6: Проверка в браузере (DevTools)

**Откройте сайт на проде и нажмите F12:**

1. **Network tab:**
   - Перезагрузите страницу (Ctrl+F5)
   - Найдите CSS файлы
   - Проверьте их размер
   - Проверьте статус (должен быть 200)

2. **Console tab:**
   - Проверьте на ошибки
   - Проверьте предупреждения

3. **Elements tab:**
   - Найдите элемент с Tailwind классами (например, `bg-slate-50`)
   - Проверьте Computed styles
   - Проверьте, применяются ли стили

4. **Sources tab:**
   - Найдите загруженные CSS/JS файлы
   - Проверьте их содержимое

---

### Шаг 7: Сравнение package-lock.json

**На проде:**
```bash
cd frontend
md5sum package-lock.json
```

**Локально:**
```powershell
cd frontend
Get-FileHash package-lock.json
```

**Если хеши отличаются:**
```bash
# На проде
rm -rf node_modules package-lock.json
npm install
```

---

### Шаг 8: Проверка Node.js версии

**На проде:**
```bash
node --version
npm --version
```

**Локально:**
```powershell
node --version
npm --version
```

**Версии должны совпадать!** Next.js 16 требует Node.js 18.17+

**Если версии отличаются:**
```bash
# Используйте nvm для управления версиями
nvm install 20
nvm use 20
```

---

## 🚨 Частые проблемы и решения

### Проблема 1: Разные версии зависимостей
**Симптомы:** Разный дизайн, ошибки в консоли
**Решение:**
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run build
```

### Проблема 2: Старая версия кода на проде
**Симптомы:** Старый дизайн, отсутствуют новые функции
**Решение:**
```bash
cd /path/to/project
git pull origin testmain
cd frontend
npm install
npm run build
sudo systemctl restart planner-frontend
```

### Проблема 3: Tailwind не генерирует стили
**Симптомы:** Нет стилей, только базовый HTML
**Решение:**
```bash
cd frontend
# Убедитесь, что файлы существуют
ls -la tailwind.config.ts postcss.config.mjs src/app/globals.css

# Пересоберите
rm -rf .next
npm run build
```

### Проблема 4: Кэш браузера
**Симптомы:** Старый дизайн даже после обновления
**Решение:**
- Очистите кэш браузера (Ctrl+Shift+Delete)
- Или откройте в режиме инкогнито
- Или добавьте `?v=$(date +%s)` к URL

### Проблема 5: Кэш на сервере (Nginx)
**Симптомы:** Старые файлы отдаются
**Решение:**
```bash
# Очистите кэш Nginx
sudo nginx -s reload

# Или отключите кэширование для статики временно
# В конфиге Nginx добавьте:
# add_header Cache-Control "no-cache, no-store, must-revalidate";
```

---

## 📝 Скрипт для автоматической проверки на проде

Создайте файл `check-production.sh`:
```bash
#!/bin/bash

echo "=== Checking versions ==="
cd frontend
npm list next react react-dom tailwindcss --depth=0

echo -e "\n=== Checking Node.js ==="
node --version
npm --version

echo -e "\n=== Checking Git ==="
cd ..
git log -1 --oneline
git status
git branch

echo -e "\n=== Checking config files ==="
cd frontend
ls -la next.config.ts tailwind.config.ts postcss.config.mjs tsconfig.json

echo -e "\n=== Checking build ==="
if [ -d ".next" ]; then
    echo ".next exists"
    du -sh .next
    echo "CSS files:"
    find .next -name "*.css" -type f | head -5
else
    echo ".next does NOT exist - need to build!"
fi

echo -e "\n=== Checking source files ==="
ls -lh src/app/globals.css src/app/layout.tsx src/app/page.tsx
```

Запустите:
```bash
chmod +x check-production.sh
./check-production.sh
```

---

## ✅ Чеклист для исправления

- [ ] Версии зависимостей одинаковые (`npm list`)
- [ ] package-lock.json одинаковый (сравнить хеши)
- [ ] Исходный код одинаковый (`git log`, `git status`)
- [ ] Все конфигурационные файлы существуют
- [ ] Сборка выполнена заново (`rm -rf .next && npm run build`)
- [ ] CSS файлы не пустые (проверить размеры)
- [ ] Node.js версия одинаковая
- [ ] Кэш браузера очищен
- [ ] Кэш Nginx очищен (если используется)
- [ ] Frontend сервис перезапущен (`sudo systemctl restart planner-frontend`)

---

## 🔧 Быстрое исправление (если ничего не помогло)

**Выполните на проде:**
```bash
cd /path/to/project

# 1. Обновите код
git pull origin testmain

# 2. Пересоберите frontend
cd frontend
rm -rf .next node_modules package-lock.json
npm install
npm run build

# 3. Перезапустите сервис
sudo systemctl restart planner-frontend

# 4. Проверьте статус
sudo systemctl status planner-frontend
```

**После этого проверьте сайт в браузере с очищенным кэшем!**

