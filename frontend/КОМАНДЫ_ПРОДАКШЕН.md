# 🔧 Команды для проверки и исправления на продакшене

## 📍 Путь к проекту
Согласно скрипту деплоя, проект находится в `/opt/planner`

---

## ✅ Шаг 1: Проверка версий зависимостей

```bash
cd /opt/planner/frontend

# Проверьте версии
npm list next react react-dom tailwindcss --depth=0
```

**Ожидаемый результат:**
```
frontend@0.1.0 /opt/planner/frontend
├── next@16.0.3
├── react@19.2.0
├── react-dom@19.2.0
└── tailwindcss@4.1.17
```

---

## ✅ Шаг 2: Проверка Git (версия кода)

```bash
# Перейдите в корень проекта
cd /opt/planner

# Проверьте последний коммит
git log -1 --oneline

# Проверьте статус (нет ли незакоммиченных изменений)
git status

# Проверьте текущую ветку
git branch

# Если нужно обновить код из репозитория
git fetch origin
git pull origin testmain  # или ваша ветка (main, master и т.д.)
```

---

## ✅ Шаг 3: Полная пересборка Frontend

```bash
# Перейдите в папку frontend
cd /opt/planner/frontend

# Удалите старую сборку и зависимости
rm -rf .next node_modules package-lock.json

# Переустановите зависимости
npm install

# Пересоберите проект
npm run build

# Проверьте, что сборка прошла успешно
if [ -d ".next" ]; then
    echo "✓ Сборка успешна!"
    du -sh .next
else
    echo "✗ Ошибка сборки!"
    exit 1
fi
```

---

## ✅ Шаг 4: Перезапуск сервиса

```bash
# Перезапустите Frontend сервис
sudo systemctl restart planner-frontend

# Проверьте статус
sudo systemctl status planner-frontend

# Если нужно посмотреть логи
sudo journalctl -u planner-frontend -n 50 --no-pager
```

---

## 🔄 Полная последовательность команд (скопируйте и выполните)

```bash
# 1. Проверка версий
cd /opt/planner/frontend
npm list next react react-dom tailwindcss --depth=0

# 2. Проверка Git
cd /opt/planner
git log -1 --oneline
git status
git branch

# 3. Обновление кода (если нужно)
# git pull origin testmain

# 4. Полная пересборка
cd /opt/planner/frontend
rm -rf .next node_modules package-lock.json
npm install
npm run build

# 5. Перезапуск сервиса
sudo systemctl restart planner-frontend
sudo systemctl status planner-frontend
```

---

## 🚨 Если проект в другом месте

Если проект находится не в `/opt/planner`, найдите правильный путь:

```bash
# Найдите где находится проект
sudo find / -name "package.json" -path "*/frontend/package.json" 2>/dev/null

# Или проверьте systemd сервис
sudo systemctl status planner-frontend | grep WorkingDirectory

# Или проверьте процессы
ps aux | grep "next start" | grep -v grep
```

Затем замените `/opt/planner` на найденный путь.

---

## 📋 Дополнительные проверки

### Проверка конфигурационных файлов
```bash
cd /opt/planner/frontend
ls -la next.config.ts tailwind.config.ts postcss.config.mjs tsconfig.json
```

### Проверка CSS файлов после сборки
```bash
cd /opt/planner/frontend
find .next -name "*.css" -type f | head -10
```

### Проверка размера сборки
```bash
cd /opt/planner/frontend
du -sh .next
du -sh node_modules
```

### Проверка переменных окружения
```bash
cd /opt/planner/frontend
cat .env.local 2>/dev/null || echo ".env.local не найден"
```

---

## ⚠️ Если что-то пошло не так

### Ошибка при npm install
```bash
# Очистите кэш npm
npm cache clean --force

# Попробуйте снова
npm install
```

### Ошибка при сборке
```bash
# Проверьте логи
npm run build 2>&1 | tee build.log

# Посмотрите последние ошибки
tail -50 build.log
```

### Сервис не запускается
```bash
# Проверьте логи
sudo journalctl -u planner-frontend -n 100 --no-pager

# Проверьте права доступа
ls -la /opt/planner/frontend/.next

# Проверьте конфигурацию сервиса
sudo systemctl cat planner-frontend
```

---

## ✅ Быстрая проверка после исправления

```bash
# 1. Проверьте что сервис работает
sudo systemctl is-active planner-frontend && echo "✓ Сервис активен" || echo "✗ Сервис не работает"

# 2. Проверьте что сборка существует
[ -d "/opt/planner/frontend/.next" ] && echo "✓ Сборка существует" || echo "✗ Сборка отсутствует"

# 3. Проверьте версии
cd /opt/planner/frontend
npm list next react react-dom tailwindcss --depth=0 | grep -E "next|react|tailwindcss"
```

