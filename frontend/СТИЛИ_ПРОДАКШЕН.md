# 🔧 Исправление различий стилей между локальной и продакшен средой

## Проблема
Стили и визуал на проде отличаются от локалки.

## ✅ Что было исправлено:

### 1. Добавлен `tailwind.config.ts`
Создан файл конфигурации для Tailwind CSS v4, который обеспечивает правильную обработку классов в production.

### 2. Исправлен `globals.css`
- Изменен `font-family` в `body` для использования CSS переменной `--font-sans`
- Теперь шрифты Geist будут применяться правильно в обоих окружениях

### 3. Обновлен `next.config.ts`
- Убрана опция `optimizeCss`, которая могла вызывать проблемы с Tailwind v4

## 🔍 Диагностика проблемы:

### Шаг 1: Проверьте загрузку шрифтов
Откройте DevTools (F12) → Network → перезагрузите страницу:
- Проверьте, загружаются ли шрифты Geist (должны быть запросы к `fonts.googleapis.com` или `fonts.gstatic.com`)
- Если шрифты не загружаются в production, проверьте настройки CORS и доступность Google Fonts

### Шаг 2: Проверьте CSS переменные
В DevTools → Console выполните:
```javascript
getComputedStyle(document.documentElement).getPropertyValue('--font-geist-sans')
getComputedStyle(document.documentElement).getPropertyValue('--background')
```
Должны вернуться значения переменных.

### Шаг 3: Проверьте Tailwind классы
В DevTools → Elements найдите элемент с Tailwind классами (например, `className="bg-slate-50"`):
- Проверьте, применяются ли стили в Computed styles
- Если стили не применяются, возможно, классы не генерируются в production

### Шаг 4: Очистите кэш
```powershell
# В папке frontend
Remove-Item -Recurse -Force .next
npm run build
```

## 🛠️ Дополнительные исправления:

### Если шрифты не загружаются в production:

1. **Проверьте доступность Google Fonts:**
   - Убедитесь, что сервер имеет доступ к интернету
   - Проверьте, не блокируются ли запросы файрволом

2. **Альтернатива: использовать локальные шрифты:**
   ```typescript
   // В layout.tsx можно использовать localFont вместо Google Fonts
   import localFont from "next/font/local";
   
   const geistSans = localFont({
     src: "./fonts/Geist-Regular.woff2",
     variable: "--font-geist-sans",
   });
   ```

### Если Tailwind классы не работают в production:

1. **Проверьте сборку:**
   ```powershell
   cd frontend
   npm run build
   ```
   Проверьте, нет ли ошибок или предупреждений.

2. **Проверьте содержимое `.next/static/css/`:**
   - Должны быть CSS файлы с Tailwind стилями
   - Если файлы пустые или маленькие, возможно, Tailwind не генерирует стили

3. **Временное решение - добавить safelist в tailwind.config.ts:**
   ```typescript
   const config: Config = {
     content: [...],
     safelist: [
       'bg-slate-50',
       'text-slate-900',
       // добавьте классы, которые не применяются
     ],
   };
   ```

### Если CSS переменные не работают:

1. **Проверьте порядок импортов в `layout.tsx`:**
   ```typescript
   import "./globals.css"; // должен быть ПЕРЕД использованием переменных
   ```

2. **Убедитесь, что `@theme inline` поддерживается:**
   - Tailwind CSS v4 должен поддерживать этот синтаксис
   - Если нет, можно использовать традиционный синтаксис:
   ```css
   @tailwind base;
   @tailwind components;
   @tailwind utilities;
   ```

## 📋 Чеклист для проверки:

- [ ] Шрифты Geist загружаются в production
- [ ] CSS переменные определены и применяются
- [ ] Tailwind классы генерируются в production build
- [ ] Кэш очищен (`.next` папка удалена и пересобрана)
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок при сборке (`npm run build`)

## 🚀 После исправлений:

1. Пересоберите проект:
   ```powershell
   cd frontend
   Remove-Item -Recurse -Force .next
   npm run build
   ```

2. Запустите production версию локально для проверки:
   ```powershell
   npm run start
   ```

3. Сравните с dev версией:
   ```powershell
   npm run dev
   ```

4. Если все работает локально, задеплойте на продакшен.

## 💡 Дополнительные советы:

- Используйте одинаковые версии зависимостей в dev и production
- Проверяйте `package-lock.json` - он должен быть закоммичен
- Убедитесь, что на продакшене используется та же версия Node.js, что и локально
- Проверьте, что переменные окружения одинаковые (если они влияют на стили)

