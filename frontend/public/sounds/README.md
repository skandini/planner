# 🔊 Звуковые файлы уведомлений

## Требуемые файлы

### `notification.mp3`
Звук воспроизводится когда приходит новое уведомление.

**Требования:**
- Формат: MP3
- Длительность: 1-3 секунды (рекомендуется)
- Громкость: средняя (автоматически снижается до 50%)

---

## Загрузка файла

1. Поместите ваш звуковой файл `notification.mp3` в эту папку:
   ```
   frontend/public/sounds/notification.mp3
   ```

2. Файл будет автоматически доступен по пути: `/sounds/notification.mp3`

3. Звук воспроизводится при получении нового уведомления (каждые 15 секунд проверка)

---

## Как это работает

1. **Polling:** Каждые 15 секунд проверяется количество непрочитанных уведомлений
2. **Триггер:** Если количество увеличилось → воспроизводится звук
3. **Громкость:** Автоматически 50% от максимальной
4. **Fallback:** Если звук не загружается/не воспроизводится → просто логируется ошибка (не ломает работу)

---

## Альтернативные форматы

Если нужны другие форматы, добавьте их и измените путь в `useNotifications.ts`:

```typescript
const audio = new Audio('/sounds/notification.wav');
// или
const audio = new Audio('/sounds/notification.ogg');
```

---

**Примечание:** Браузеры могут блокировать автовоспроизведение звука пока пользователь не взаимодействовал со страницей. Это нормальное поведение для безопасности.

