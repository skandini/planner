# Следующие шаги и план действий

## 📊 Текущее состояние

### База данных
- **По умолчанию:** SQLite (`sqlite:///../calendar.db`)
- **Для production:** Рекомендуется PostgreSQL
- **Connection pooling:** Настроен для PostgreSQL (20 базовых + 40 overflow)

### Выполненные улучшения
- ✅ Фаза 1: Критические исправления (завершена)
- ✅ Фаза 2: Производительность (частично - кеширование, оптимизация, rate limiting)

---

## 🔍 Как проверить, что все работает

### Быстрая проверка (5 минут)

1. **Установите зависимости:**
   ```bash
   cd backend
   pip install -r requirements.txt
   ```

2. **Примените миграции:**
   ```bash
   alembic upgrade head
   ```

3. **Запустите сервер:**
   ```bash
   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```

4. **Запустите автоматическую проверку:**
   ```bash
   python check_system.py
   ```

5. **Проверьте в браузере:**
   - http://localhost:8000/docs - Swagger документация
   - http://localhost:8000/api/v1/health/ - Health check
   - http://localhost:8000/api/v1/health/ready - Readiness check

### Детальная проверка

См. файл `TESTING_PLAN.md` для полного плана тестирования.

---

## 📋 Следующий план действий

### Приоритет 1: Тестирование и проверка (1-2 дня)

1. **Запустить и проверить систему:**
   - [ ] Установить зависимости
   - [ ] Применить миграции
   - [ ] Запустить сервер
   - [ ] Выполнить автоматическую проверку (`check_system.py`)
   - [ ] Протестировать основные endpoints вручную

2. **Проверить производительность:**
   - [ ] Проверить работу кеширования (логи должны показывать меньше запросов к БД)
   - [ ] Проверить rate limiting (попробовать превысить лимиты)
   - [ ] Проверить индексы БД (для ускорения запросов)

3. **Проверить безопасность:**
   - [ ] Проверить CORS настройки
   - [ ] Проверить security headers
   - [ ] Проверить валидацию SECRET_KEY

### Приоритет 2: Миграция на PostgreSQL (для production) (1 день)

**Если планируете использовать на production:**

1. **Установить PostgreSQL:**
   ```bash
   # Ubuntu/Debian
   sudo apt install postgresql postgresql-contrib
   ```

2. **Создать базу данных:**
   ```bash
   sudo -u postgres psql
   CREATE DATABASE planner_db;
   CREATE USER planner_user WITH PASSWORD 'your_password';
   GRANT ALL PRIVILEGES ON DATABASE planner_db TO planner_user;
   ```

3. **Обновить .env:**
   ```env
   DATABASE_URL=postgresql://planner_user:your_password@localhost/planner_db
   ENVIRONMENT=production
   SECRET_KEY=your_generated_secret_key_here
   ```

4. **Применить миграции:**
   ```bash
   alembic upgrade head
   ```

### Приоритет 3: Дополнительные улучшения (опционально)

1. **Кеширование для календарей и событий:**
   - Добавить кеш для часто запрашиваемых данных
   - TTL: 5-15 минут

2. **Асинхронная обработка уведомлений:**
   - Использовать BackgroundTasks или Celery
   - Не блокировать основной запрос

3. **Мониторинг и метрики:**
   - Prometheus метрики
   - Расширенное логирование
   - Alerting при ошибках

4. **Резервное копирование:**
   - Автоматический backup БД
   - Backup файлов (или использовать S3)

---

## 🚀 Деплой на production сервер

### Если все проверки пройдены:

1. **Подготовка сервера:**
   - См. `DEPLOYMENT_UBUNTU24.md` для детальных инструкций
   - Или используйте Docker (если настроен)

2. **Настройка .env на сервере:**
   ```env
   DATABASE_URL=postgresql://planner_user:password@localhost/planner_db
   ENVIRONMENT=production
   SECRET_KEY=<сгенерированный_ключ>
   BACKEND_CORS_ORIGINS=https://calendar.corestone.ru,https://www.calendar.corestone.ru
   ```

3. **Применить миграции:**
   ```bash
   alembic upgrade head
   ```

4. **Запустить сервис:**
   ```bash
   # Через systemd (см. DEPLOYMENT_UBUNTU24.md)
   sudo systemctl start planner-backend
   sudo systemctl enable planner-backend
   ```

5. **Настроить Nginx и SSL:**
   - См. `DEPLOYMENT_UBUNTU24.md`

---

## ⚠️ Важные замечания

### Для production обязательно:

1. **Измените SECRET_KEY:**
   ```bash
   openssl rand -hex 32
   ```
   Добавьте результат в `.env`

2. **Используйте PostgreSQL:**
   - SQLite не подходит для 200+ пользователей
   - Настройте connection pooling (уже настроен)

3. **Настройте CORS:**
   - Укажите конкретные домены в `BACKEND_CORS_ORIGINS`
   - Не используйте `["*"]` в production

4. **Настройте мониторинг:**
   - Health checks уже настроены
   - Добавьте логирование в файл
   - Настройте алерты

---

## 📝 Чеклист перед деплоем

- [ ] Все тесты пройдены (`check_system.py`)
- [ ] Миграции применены
- [ ] SECRET_KEY изменен
- [ ] PostgreSQL настроен (для production)
- [ ] CORS настроен правильно
- [ ] Rate limiting работает
- [ ] Health checks работают
- [ ] Логирование настроено
- [ ] Резервное копирование настроено
- [ ] SSL сертификаты установлены
- [ ] Nginx настроен
- [ ] Мониторинг настроен

---

## 🆘 Если что-то не работает

1. **Проверьте логи:**
   ```bash
   # Логи сервера
   tail -f /var/log/planner-backend.log
   
   # Или если запускаете вручную
   # Логи будут в консоли
   ```

2. **Проверьте миграции:**
   ```bash
   alembic current
   alembic history
   ```

3. **Проверьте подключение к БД:**
   ```bash
   # Для PostgreSQL
   psql -U planner_user -d planner_db -c "SELECT 1;"
   
   # Для SQLite
   sqlite3 calendar.db "SELECT 1;"
   ```

4. **Проверьте конфигурацию:**
   ```bash
   python -c "from app.core.config import settings; print(settings.DATABASE_URL)"
   ```

---

## 📚 Дополнительные ресурсы

- `TESTING_PLAN.md` - Детальный план тестирования
- `DEPLOYMENT_UBUNTU24.md` - Инструкции по деплою
- `CHANGELOG_PHASE1.md` - Изменения Фазы 1
- `CHANGELOG_PHASE2.md` - Изменения Фазы 2
- `IMPROVEMENTS_IMPLEMENTED.md` - Все реализованные улучшения


