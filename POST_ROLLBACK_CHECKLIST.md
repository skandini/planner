# Чеклист после отката до версии без WebSocket

## ✅ Выполнено

- [x] Откат к коммиту `a070e31` (до добавления WebSocket)
- [x] Создана резервная ветка `backup-before-rollback` с предыдущим состоянием
- [x] Проверено, что `python-socketio` не в `requirements.txt`
- [x] Проверено, что `socket.io-client` не в `package.json`

## 🔍 Что нужно проверить

### 1. Удалить файлы WebSocket (если они остались)

Проверьте наличие этих файлов и удалите их, если есть:

**Backend:**
- `backend/app/websocket/` (вся папка)
- Импорты WebSocket в `backend/app/main.py`
- Импорты WebSocket в `backend/app/api/v1/events.py`
- Импорты WebSocket в `backend/app/services/notifications.py`

**Frontend:**
- `frontend/src/hooks/useWebSocket.ts`
- Импорты WebSocket в `frontend/src/app/page.tsx`
- Импорты WebSocket в других компонентах

### 2. Удалить зависимости (если установлены)

```bash
# Backend
cd backend
pip uninstall python-socketio -y

# Frontend
cd frontend
npm uninstall socket.io-client
```

### 3. Перезапустить серверы

```bash
# Backend
cd backend
uvicorn app.main:app --reload

# Frontend
cd frontend
npm run dev
```

### 4. Проверить функциональность

- [ ] Создание событий работает
- [ ] Приглашения участников работают
- [ ] Уведомления приходят (через polling)
- [ ] Обновление статусов участников работает
- [ ] Календарь отображается корректно

## 🔄 Если нужно вернуться к версии с WebSocket

```bash
git checkout backup-before-rollback
```

## 📝 Примечания

- Все изменения после `a070e31` удалены
- Резервная копия сохранена в ветке `backup-before-rollback`
- Если нужно восстановить какие-то конкретные изменения, можно использовать `git cherry-pick`


