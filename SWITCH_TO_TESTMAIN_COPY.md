# Инструкция по переключению прод на ветку testmain-copy

## ✅ Подтверждение
Ветка `testmain-copy` создана и является точной копией `testmain`:
- Ветка создана: ✅
- Отправлена на GitHub: ✅  
- Идентична testmain: ✅ (проверено через `git diff`)

## 🔄 Переключение прода

### Вариант 1: Если деплой настроен на сервере (SSH)

На сервере выполните:
```bash
cd /path/to/your/project

# Получить последние изменения
git fetch origin

# Переключиться на testmain-copy
git checkout testmain-copy

# Обновить до последней версии
git pull origin testmain-copy

# Запустить деплой (зависит от вашей настройки)
# Например:
# - npm run build (для фронтенда)
# - docker-compose up -d (если используется Docker)
# - или другие команды деплоя
```

### Вариант 2: Если используется CI/CD (GitHub Actions, GitLab CI и т.д.)

Обновите настройки CI/CD:
1. Найдите файл конфигурации (`.github/workflows/*.yml`, `.gitlab-ci.yml` и т.д.)
2. Измените указание ветки с `testmain` на `testmain-copy`
3. Запустите пайплайн

### Вариант 3: Если используется хостинг (Vercel, Netlify и т.д.)

1. Зайдите в настройки проекта
2. Найдите настройку "Production Branch" или "Deploy Branch"
3. Измените с `testmain` на `testmain-copy`
4. Сохраните и дождитесь деплоя

## 📝 Проверка после переключения

После переключения проверьте:
1. ✅ Сайт работает корректно
2. ✅ Все функции доступны
3. ✅ Нет ошибок в логах

## 🔗 Ссылки

- Ветка на GitHub: `origin/testmain-copy`
- Создать PR: https://github.com/skandini/planner/pull/new/testmain-copy

