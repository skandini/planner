# Безопасный процесс деплоя в продакшн

## ❌ Почему НЕ стоит вносить изменения напрямую в продакшн:

1. **Риск сломать работающую систему** - если что-то пойдет не так, пользователи пострадают
2. **Нет возможности откатить** - сложнее вернуться к рабочей версии
3. **Нет истории изменений** - изменения могут быть потеряны
4. **Нет тестирования** - баги попадут сразу к пользователям
5. **Сложно отследить проблемы** - не понятно что и когда изменилось

## ✅ Рекомендуемый процесс:

### Вариант 1: Через Git (рекомендуется)

```bash
# ЛОКАЛЬНО:
# 1. Внести изменения
# 2. Протестировать локально
# 3. Закоммитить
git add .
git commit -m "Описание изменений"
git push origin testmain

# НА СЕРВЕРЕ:
# 4. Обновить код
cd /opt/planner
git pull origin testmain

# 5. Перезапустить сервисы
sudo systemctl restart planner-backend
sudo systemctl restart planner-celery-worker
cd frontend && npm run build && sudo systemctl restart planner-frontend
```

**Преимущества:**
- ✅ История изменений в Git
- ✅ Можно откатить (git revert)
- ✅ Можно протестировать локально
- ✅ Все изменения отслеживаются

### Вариант 2: Создать ветку для тестирования

```bash
# ЛОКАЛЬНО:
# 1. Создать ветку для изменений
git checkout -b feature/название-функции

# 2. Внести изменения и протестировать
# ... делаете изменения ...

# 3. Закоммитить
git add .
git commit -m "Описание"
git push origin feature/название-функции

# 4. На сервере протестировать на отдельной ветке
# (если нужно)

# 5. После тестирования - merge в testmain
git checkout testmain
git merge feature/название-функции
git push origin testmain
```

## 🚨 Если ОБЯЗАТЕЛЬНО нужно изменить напрямую на сервере:

### Экстренные исправления (hotfix):

```bash
# НА СЕРВЕРЕ:
cd /opt/planner

# 1. Сохранить текущее состояние
git stash
# Или создать бэкап
cp -r backend/app backend/app.backup

# 2. Внести изменения
# ... редактируете файлы ...

# 3. Протестировать
# ... проверяете что работает ...

# 4. Закоммитить изменения
git add .
git commit -m "Hotfix: описание исправления"
git push origin testmain

# 5. Обновить локально
# ЛОКАЛЬНО:
git pull origin testmain
```

## 📋 Чеклист перед деплоем:

- [ ] Изменения протестированы локально
- [ ] Код закоммичен в Git
- [ ] Создан бэкап базы данных (если нужно)
- [ ] План отката подготовлен
- [ ] Время деплоя выбрано (минимум пользователей)
- [ ] Мониторинг готов (проверить логи после деплоя)

## 🔄 Процесс отката (rollback):

Если что-то пошло не так:

```bash
# НА СЕРВЕРЕ:
cd /opt/planner

# 1. Откатить к предыдущему коммиту
git log --oneline -5  # найти нужный коммит
git reset --hard <commit-hash>

# Или откатить к тегу MVP-2026
git reset --hard MVP-2026

# 2. Перезапустить сервисы
sudo systemctl restart planner-backend
sudo systemctl restart planner-celery-worker
cd frontend && npm run build && sudo systemctl restart planner-frontend
```

## 💡 Рекомендации:

### Для мелких изменений:
1. Изменить локально
2. Протестировать
3. Закоммитить и запушить
4. Обновить на сервере через `git pull`

### Для крупных изменений:
1. Создать feature-ветку
2. Разработать и протестировать
3. Merge в testmain
4. Деплой на сервер

### Для критических исправлений:
1. Создать hotfix-ветку
2. Исправить и протестировать
3. Merge в testmain
4. Немедленный деплой

## ⚠️ Что НИКОГДА не делать:

- ❌ Редактировать файлы напрямую на сервере без коммита в Git
- ❌ Деплоить без тестирования
- ❌ Деплоить в рабочее время без крайней необходимости
- ❌ Деплоить без возможности отката
- ❌ Изменять базу данных без бэкапа

## 🛡️ Безопасный workflow:

```
Локальная разработка
    ↓
Тестирование локально
    ↓
Коммит в Git
    ↓
Push в GitHub
    ↓
Деплой на сервер (git pull)
    ↓
Проверка работы
    ↓
Готово!
```

## 📝 Пример безопасного деплоя:

```bash
# ЛОКАЛЬНО:
# 1. Обновить код
git pull origin testmain

# 2. Внести изменения
# ... редактируете файлы ...

# 3. Протестировать локально
npm run dev  # для фронтенда
# Проверить что все работает

# 4. Закоммитить
git add .
git commit -m "Добавлена функция X"
git push origin testmain

# НА СЕРВЕРЕ:
# 5. Обновить код
cd /opt/planner
git pull origin testmain

# 6. Проверить изменения
git log --oneline -1
git diff HEAD~1  # посмотреть что изменилось

# 7. Перезапустить сервисы
sudo systemctl restart planner-backend
sudo systemctl restart planner-celery-worker

# 8. Если изменился фронтенд
cd frontend
npm run build
sudo systemctl restart planner-frontend

# 9. Проверить логи
sudo journalctl -u planner-backend --since "1 minute ago" -n 20
sudo journalctl -u planner-frontend --since "1 minute ago" -n 20

# 10. Проверить в браузере
# Открыть приложение и убедиться что все работает
```

## 🎯 Итог:

**Рекомендуется:** Всегда работать через Git - локально разрабатывать, тестировать, коммитить, и только потом деплоить на сервер через `git pull`.

**Допустимо:** Прямые изменения на сервере только для экстренных hotfix, но обязательно с последующим коммитом в Git.


