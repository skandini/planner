# Инструкция по восстановлению проекта

## 🚨 Если проект не запускается локально

### Быстрое решение (рекомендуется)

**Просто запустите скрипт полной пересборки:**

```powershell
.\rebuild-project.ps1
```

Этот скрипт автоматически:
- ✅ Удалит старые окружения (`.venv`, `node_modules`)
- ✅ Пересоздаст все с нуля
- ✅ Установит все зависимости
- ✅ Настроит конфигурацию
- ✅ Применит миграции БД
- ✅ **НЕ удалит базу данных** (ваши данные сохранятся!)

После выполнения скрипта запустите проект:
```powershell
.\start-local.ps1
```

---

## 📋 Ручное восстановление (если скрипт не помог)

### Шаг 1: Очистка Backend

```powershell
cd backend

# Удалить виртуальное окружение
Remove-Item -Recurse -Force .venv

# Удалить кэш Python
Get-ChildItem -Path . -Recurse -Filter "__pycache__" | Remove-Item -Recurse -Force
Get-ChildItem -Path . -Recurse -Filter "*.pyc" | Remove-Item -Force

# Удалить старый .env (создадим новый)
Remove-Item -Force .env -ErrorAction SilentlyContinue
```

### Шаг 2: Пересоздание Backend

```powershell
# Создать новое виртуальное окружение
python -m venv .venv

# Активировать
.\.venv\Scripts\Activate.ps1

# Обновить pip
python -m pip install --upgrade pip

# Установить зависимости
pip install -r requirements.txt

# Создать .env
Copy-Item env.example.txt .env

# Применить миграции
alembic upgrade head
```

### Шаг 3: Очистка Frontend

```powershell
cd ..\frontend

# Удалить node_modules
Remove-Item -Recurse -Force node_modules

# Удалить .next (если есть)
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue

# Удалить старый .env.local
Remove-Item -Force .env.local -ErrorAction SilentlyContinue
```

### Шаг 4: Пересоздание Frontend

```powershell
# Установить зависимости
npm install

# Создать .env.local
"NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1" | Out-File -FilePath ".env.local" -Encoding utf8
```

### Шаг 5: Запуск

```powershell
cd ..

# Запустить все
.\start-local.ps1
```

---

## 🔍 Диагностика проблем

### Проблема: "Python не найден"
**Решение:**
- Установите Python 3.10+ с https://www.python.org/downloads/
- При установке отметьте "Add Python to PATH"
- Перезапустите PowerShell

### Проблема: "Node.js не найден"
**Решение:**
- Установите Node.js 20+ с https://nodejs.org/
- Перезапустите PowerShell

### Проблема: "Порт 8000 занят"
**Решение:**
```powershell
# Найти процесс на порту 8000
netstat -ano | findstr :8000

# Остановить процесс (замените PID на реальный)
taskkill /PID <PID> /F
```

### Проблема: "Порт 3000 занят"
**Решение:**
```powershell
# Найти процесс на порту 3000
netstat -ano | findstr :3000

# Остановить процесс
taskkill /PID <PID> /F
```

### Проблема: "Ошибка миграций"
**Решение:**
```powershell
cd backend
.\.venv\Scripts\Activate.ps1

# Попробовать применить миграции
alembic upgrade head

# Если не помогло, попробовать инициализацию напрямую
python -c "from app.db import init_db; init_db()"
```

### Проблема: "Отказано в доступе" / "Access Denied" при установке зависимостей
**Решение:**
```powershell
# Остановить все процессы Python
Get-Process python -ErrorAction SilentlyContinue | Stop-Process -Force

# Или используйте готовый скрипт
.\fix-locked-files.ps1

# Затем снова запустите пересборку
.\rebuild-project.ps1
```

**Причина:** Файлы в `.venv` заблокированы запущенными процессами Python (uvicorn, celery и т.д.)

### Проблема: "ModuleNotFoundError"
**Решение:**
```powershell
cd backend
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

### Проблема: "npm ERR!"
**Решение:**
```powershell
cd frontend

# Очистить кэш npm
npm cache clean --force

# Удалить node_modules и package-lock.json
Remove-Item -Recurse -Force node_modules
Remove-Item -Force package-lock.json

# Переустановить
npm install
```

---

## 🗄️ Работа с базой данных

### База данных НЕ удаляется при пересборке!

Файл `calendar.db` находится в корне проекта и сохраняется при пересборке.

### Если нужно пересоздать БД с нуля:

```powershell
# ВАЖНО: Это удалит все данные!
cd backend
.\.venv\Scripts\Activate.ps1

# Удалить старую БД
Remove-Item ..\calendar.db -ErrorAction SilentlyContinue

# Применить миграции (создаст новую БД)
alembic upgrade head
```

### Создание тестовых пользователей:

```powershell
cd backend
.\.venv\Scripts\Activate.ps1
python scripts\create_user.py
```

---

## ✅ Проверка что все работает

1. **Backend:**
   ```powershell
   # Откройте в браузере
   http://localhost:8000/api/v1/health
   # Должно вернуть: {"status":"ok"}
   ```

2. **Frontend:**
   ```powershell
   # Откройте в браузере
   http://localhost:3000
   # Должна открыться страница входа
   ```

3. **API документация:**
   ```powershell
   # Откройте в браузере
   http://localhost:8000/docs
   # Должен открыться Swagger UI
   ```

---

## 🔄 Если ничего не помогло

1. **Проверьте версии:**
   ```powershell
   python --version  # Должно быть 3.10+
   node --version     # Должно быть 20+
   ```

2. **Проверьте что вы в правильной ветке:**
   ```powershell
   git branch
   # Должно быть: * testmain
   ```

3. **Проверьте что нет незакоммиченных изменений:**
   ```powershell
   git status
   ```

4. **Если нужно, сделайте чистую проверку:**
   ```powershell
   # Сохраните изменения
   git stash
   
   # Переключитесь на testmain
   git checkout testmain
   
   # Обновите
   git pull origin testmain
   
   # Запустите пересборку
   .\rebuild-project.ps1
   ```

---

## 📞 Дополнительная помощь

Если проблема не решается:
1. Проверьте логи ошибок в консоли
2. Убедитесь что все зависимости установлены
3. Проверьте что порты 8000 и 3000 свободны
4. Убедитесь что база данных не повреждена

---

## 🎯 Быстрая справка

| Действие | Команда |
|----------|---------|
| Полная пересборка | `.\rebuild-project.ps1` |
| Запуск проекта | `.\start-local.ps1` |
| Запуск только backend | `cd backend; .\start-local.ps1` |
| Запуск только frontend | `cd frontend; .\start-local.ps1` |
| Проверка backend | Откройте http://localhost:8000/api/v1/health |
| Проверка frontend | Откройте http://localhost:3000 |

---

**Помните:** База данных `calendar.db` НЕ удаляется при пересборке, ваши данные в безопасности! 🛡️

