# 📅 Красивое отображение пересекающихся событий

## 📋 Обзор функционала

Реализован алгоритм автоматического распределения пересекающихся событий по колонкам для красивого и читаемого отображения в календаре.

**Проблема:** События, которые пересекаются по времени, накладывались друг на друга, затрудняя чтение календаря.

**Решение:** Алгоритм автоматически определяет пересекающиеся события и размещает их в отдельных колонках рядом друг с другом.

---

## 🎯 Результат

### До:
```
┌─────────────┐
│ Событие 1   │  ← Накладываются
│             │
│ Событие 2   │  ← друг на друга
└─────────────┘
```

### После:
```
┌──────┬──────┐
│ Соб. │ Соб. │  ← Отображаются
│  1   │  2   │  ← рядом в колонках
└──────┴──────┘
```

---

## 🏗️ Архитектура решения

### 1. Алгоритм расчета позиций

**Файл:** `frontend/src/lib/utils/eventLayout.ts`

**Основные функции:**

- `calculateEventLayout()` - главная функция расчета позиций
- `getEventPositionStyles()` - преобразование позиций в CSS стили

**Алгоритм работы:**

1. **Группировка:** Находит группы пересекающихся событий
2. **Распределение:** Для каждой группы распределяет события по колонкам
3. **Оптимизация:** События, которые могут занять несколько колонок, расширяются

**Пример:**

```typescript
// Входные данные
const events = [
  { id: "1", start: "09:00", end: "10:00" },
  { id: "2", start: "09:30", end: "10:30" }, // Пересекается с 1
  { id: "3", start: "11:00", end: "12:00" }, // Не пересекается
];

// Результат
const layout = calculateEventLayout(events);
// {
//   "1": { column: 0, totalColumns: 2 },
//   "2": { column: 1, totalColumns: 2 },
//   "3": { column: 0, totalColumns: 1 }
// }
```

### 2. Интеграция в компоненты

**Обновленные файлы:**

- `frontend/src/components/calendar/WeekView.tsx`
- `frontend/src/components/calendar/WeekViewEnhanced.tsx`

**Изменения:**

1. **Расчет layout для каждого дня:**

```typescript
const eventLayoutMap = calculateEventLayout(
  dayEvents.map(event => ({
    id: event.id,
    start: parseUTC(event.starts_at),
    end: parseUTC(event.ends_at),
  }))
);
```

2. **Применение стилей к событиям:**

```typescript
const layout = eventLayoutMap?.get(event.id);
const positionStyles = layout 
  ? getEventPositionStyles(layout, { gapSize: 2, minWidth: 60 })
  : { left: '2px', width: 'calc(100% - 4px)' };

// Применяем к элементу
style={{
  left: positionStyles.left,
  width: positionStyles.width,
  // ... другие стили
}}
```

---

## 🔧 Технические детали

### Параметры конфигурации

```typescript
getEventPositionStyles(layout, {
  gapSize: 2,      // Отступ между событиями в px
  minWidth: 60,    // Минимальная ширина события в px
});
```

### Поддержка различных сценариев

✅ **2 пересекающихся события** → 2 колонки по 50% ширины  
✅ **3 пересекающихся события** → 3 колонки по 33% ширины  
✅ **Частичное пересечение** → Оптимизация с расширением событий  
✅ **Событие в одиночестве** → Занимает всю ширину  
✅ **События разной длительности** → Корректное позиционирование

### Оптимизация производительности

- **Мемоизация:** Расчет выполняется только при изменении событий
- **О(n log n):** Сложность алгоритма сортировки
- **Локальный расчет:** Каждый день рассчитывается независимо

---

## 📊 Примеры использования

### Пример 1: Два события одновременно

```
09:00 - 10:00: Планерка отдела ИТ
09:30 - 10:30: Встреча с клиентом

Результат:
┌──────────────┬──────────────┐
│ Планерка ИТ  │ Встреча с    │
│              │ клиентом     │
└──────────────┴──────────────┘
```

### Пример 2: Три события с частичным пересечением

```
09:00 - 10:00: Событие A
09:30 - 10:30: Событие B (пересекается с A)
10:00 - 11:00: Событие C (пересекается с B)

Результат:
┌──────┬──────┬──────┐
│  A   │  B   │  C   │
│      │      │      │
└──────┴──────┴──────┘
```

### Пример 3: События без пересечений

```
09:00 - 10:00: Событие 1
11:00 - 12:00: Событие 2

Результат:
┌────────────────────┐
│ Событие 1          │
└────────────────────┘
     (пустое время)
┌────────────────────┐
│ Событие 2          │
└────────────────────┘
```

---

## 🧪 Тестирование

### Проверка функционала

1. Откройте календарь в браузере
2. Создайте несколько событий в одно время
3. Убедитесь, что события отображаются в колонках рядом друг с другом
4. Проверьте различные сценарии:
   - 2 события одновременно
   - 3+ события одновременно
   - События разной длительности
   - Частичное пересечение событий

### Визуальная проверка

- ✅ События не накладываются друг на друга
- ✅ Между событиями есть небольшой отступ (2px)
- ✅ Цвета и стили событий сохранены
- ✅ Hover эффекты работают корректно
- ✅ Drag & Drop работает корректно

---

## 🚀 Деплой

### Локальная проверка

```powershell
cd c:\testprj\frontend
npm run dev
```

Откройте http://localhost:3000 и проверьте календарь.

### Деплой на сервер

```bash
# 1. Коммит изменений
cd c:\testprj
git add frontend/src/lib/utils/eventLayout.ts
git add frontend/src/components/calendar/WeekView.tsx
git add frontend/src/components/calendar/WeekViewEnhanced.tsx
git add FEATURE_EVENT_OVERLAP_LAYOUT.md

git commit -m "feat: красивое отображение пересекающихся событий в календаре"
git push origin refactor/split-page-tsx

# 2. На сервере
ssh root@calendar.corestone.ru
cd /opt/planner
git pull origin refactor/split-page-tsx
cd frontend
npm run build
systemctl restart planner-frontend

# 3. Проверка
curl https://calendar.corestone.ru
```

---

## 📝 Технические требования

### Браузеры

✅ Chrome 90+  
✅ Firefox 88+  
✅ Safari 14+  
✅ Edge 90+

### Зависимости

Нет новых зависимостей! Используются только встроенные возможности TypeScript и React.

---

## 🔍 Известные ограничения

1. **Максимум колонок:** Алгоритм оптимален для 2-4 пересекающихся событий. При большем количестве события становятся узкими.
   - **Решение:** События автоматически ужимаются, сохраняя минимальную ширину 60px.

2. **Мобильные устройства:** На маленьких экранах события могут быть менее читаемыми при большом количестве пересечений.
   - **Решение:** На мобильных можно переключиться на список или day view.

---

## 🎨 Кастомизация

### Изменение отступов между событиями

```typescript
// В WeekView.tsx или WeekViewEnhanced.tsx
const positionStyles = layout 
  ? getEventPositionStyles(layout, { 
      gapSize: 4,      // Увеличить до 4px
      minWidth: 80     // Увеличить минимальную ширину
    })
  : { left: '2px', width: 'calc(100% - 4px)' };
```

### Изменение визуального стиля

CSS классы остаются прежними, можно кастомизировать через Tailwind или добавить дополнительные классы.

---

## 🐛 Troubleshooting

### События всё еще накладываются

**Проблема:** Алгоритм не применяется  
**Решение:** 
1. Проверьте, что импортирован `eventLayout.ts`
2. Проверьте консоль браузера на ошибки
3. Убедитесь, что `eventLayoutMap` передается в рендер

### События слишком узкие

**Проблема:** Много пересекающихся событий  
**Решение:** Увеличьте параметр `minWidth` или используйте другой view

### Производительность

**Проблема:** Календарь медленно работает  
**Решение:** Алгоритм уже оптимизирован. Проверьте количество событий в календаре (рекомендуется до 200 событий одновременно).

---

## 📚 Дополнительные материалы

### Алгоритм календарного layout

Реализация основана на классическом алгоритме для календарей (используется в Google Calendar, Outlook).

**Ссылки:**
- [Calendar Layout Algorithm](https://stackoverflow.com/questions/11311410/visualization-of-calendar-events-algorithm-to-layout-events-with-maximum-width)
- [Event Scheduling Visualization](https://en.wikipedia.org/wiki/Interval_scheduling)

---

**Дата создания:** 16 января 2026  
**Версия:** 1.0  
**Автор:** AI Assistant + User

---

✅ **Готово к использованию!**

