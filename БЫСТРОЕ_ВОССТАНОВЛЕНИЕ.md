# 🚀 Быстрое восстановление проекта

## Если проект не запускается - выполните 2 команды:

### ⚠️ ВАЖНО: Запускайте из корня проекта (`c:\testprj`)

### 1. Полная пересборка проекта:
```powershell
# Убедитесь что вы в корне проекта
cd c:\testprj

# Запустите пересборку
.\rebuild-project.ps1
```

### 2. Запуск проекта:
```powershell
# Из корня проекта
.\start-local.ps1
```

**Готово!** 🎉

---

## Что делает rebuild-project.ps1?

✅ Удаляет старые окружения (`.venv`, `node_modules`)  
✅ Пересоздает все с нуля  
✅ Устанавливает все зависимости  
✅ Настраивает конфигурацию  
✅ Применяет миграции БД  
✅ **НЕ удаляет базу данных** (ваши данные в безопасности!)

---

## Проверка что все работает:

- **Backend:** http://localhost:8000/api/v1/health
- **Frontend:** http://localhost:3000  
- **API Docs:** http://localhost:8000/docs

---

## Если возникла ошибка "Отказано в доступе" (Access Denied):

Это значит, что файлы заблокированы запущенными процессами. Выполните:

```powershell
.\fix-locked-files.ps1
```

Затем снова запустите:
```powershell
.\rebuild-project.ps1
```

## Если что-то пошло не так:

Смотрите подробную инструкцию: `ВОССТАНОВЛЕНИЕ_ПРОЕКТА.md`

---

**Важно:** Вы на ветке `testmain` - все изменения сохранятся! ✅

